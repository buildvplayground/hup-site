# Deploy Vercel — HUP Governança
Site estático (HTML + CSS + JS vanilla). Root Directory = `deploy-vercel`, Framework = Other/static.
CLI: `cd deploy-vercel && vercel --prod`. WhatsApp real já configurado (5511993353728).
